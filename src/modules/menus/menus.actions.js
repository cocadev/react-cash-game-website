import { createAction } from "redux-act";

export const showMenu = createAction("show menu");
export const hideMenu = createAction("hide menu");
export const setTabIndex = createAction("set tab index");
export const nextMenuScreen = createAction("next menu screen");

export const showLootBox = createAction("show loot box");
export const hideLootBox = createAction("hide loot box");

